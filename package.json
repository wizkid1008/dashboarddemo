{
  "name": "dashboarddemo",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci --ignore-scripts && npm --prefix frontend run build && node scripts/copy-frontend-dist.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
