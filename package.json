{
  "name": "dashboarddemo",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build && node scripts/copy-frontend-dist.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
